# 更新日志

本项目的所有重要变更都记录在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-07-28

### 新增
- `scripts/preview.py` — 成品预览图。按 `recipe.json` 重跑量化,把每格画成
  一颗带孔的圆豆,用来在开拼之前判断成品像不像。图纸写色号给人照着拼,
  预览图不写字只看效果,两者用的是同一份量化结果。
- `recipe.json` — 每次生成都归档本次参数与原图,配合 `source.<ext>` 即可原样重跑,
  不必记住当初敲了哪些选项。
- 图纸标题栏加成品厘米尺寸与用板数(midi 豆 5mm,标准方板 29x29)。
  店铺报价、包装、发货都按这两个走。
- `--crop-subject` — 去背景后裁到主体外接框,把格子预算全花在主体上。
  同样的 `--max-side` 能换到明显更高的有效分辨率。
- `--fill` — 中心裁到目标比例铺满整板,而不是留透明边。裁剪中心取主体外接框中心,
  不是画面几何中心,因为主体常常不居中。
- 新增规格预设 `87x87` / `58x87` / `87x58`(九拼)。
- `scripts/wb.py` — 白点校正。暖调逆光照片里白色物体的像素本身就是淡驼色,
  色卡匹配会忠实地配成褐豆,拼出来是一坨土黄。取主体最亮的一撮像素当白点参考,
  按通道增益拉回中性。
- `scripts/keep_main.py` — 只保留 alpha 蒙版里的最大连通域,剔除主体旁的零碎
  (岩石、地面)。
- `scripts/silhouette.py` — 从 `pattern.png` 反推主体轮廓并输出 ASCII 缩略图,
  源图丢失时用来校验形状。
- `scripts/make_showcase.py` 与 `examples/showcase.png` — README 用的对比展示图。
- 效果展示:海豹石雕佛像与十字架两张实拍照的 MARD 版图纸与成品预览。

### 变更
- 自定义 `--size` 若正好是单板 29 的整数倍,现在也会画板界线。顾客按板拼、按板买,
  没有板界线不知道在哪儿分板。非整数倍才没有板可对齐,不画。

### 修复
- 过滤占位色号。源数据里 `-` 表示"该品牌无此色",此前会被当成真色号收进调色板,
  出现在图纸和备料表里,而顾客买不到这个"色号",它还会抢走匹配、挤掉真实近似色。
  现在 `build_palettes.py` 构建期过滤,`load_palette` 运行期兜底再过滤一次。
- `palettes/manman.json` 移除占位色号 `-`(`#55514C`)。注意 MARD 的 `R13` 就是这个色,
  漫漫确实没有这颗豆,MARD 有,所以同一张图用两套色卡结果会有差异,这是正确行为。

## [0.1.0] - 2026-07-24

### 新增
- 首个版本。图片转拼豆图纸流水线:像素化 + 按品牌色卡做 CIEDE2000 感知色差匹配,
  产出带色号的网格图纸、备料清单(CSV/PNG)、可打印 PDF。
- 五套品牌色卡:咪小窝 / MARD / COCO / 漫漫 / 盼盼,各约 291 色。
- 精细 / 简易两档预设(分辨率 + 限色 + 去杂的组合)。
- Floyd-Steinberg 抖动(`--dither`)、杂点清理(`--despeckle`)、限色(`--max-colors`)。
- 去背景:轻量洪水填充,或可选 `rembg` AI 抠图。
- 作为 Claude Code Skill 使用(`SKILL.md`)。
